version: "2.0"

services:

  gsoa:
    image: shelleymac/gsoa_docker
    ports:
      - "5000:5000"
    volumes:
      - /home/smacneil/gsoa_docker:/app
      - /home/smacneil/gsoa_data:/data
    links:
      - redis

  redis:
    image: redis
    ports:
      - "6379:6379"
